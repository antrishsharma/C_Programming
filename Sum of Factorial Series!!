#include <stdio.h>

int main() {
    int n, i;
    long long fact = 1, sum = 0;

    scanf("%d", &n);

    for(i = 1; i <= n; i++) {
        fact = fact * i;   // calculate factorial
        sum = sum + fact; // add to sum
    }

    printf("%lld", sum);
    return 0;
}

